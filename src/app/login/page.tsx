'use client';
import Link from 'next/link'; import {useState} from 'react'; import {createClient} from '@/lib/supabase/client';
export default function Login(){const [msg,setMsg]=useState(''); async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const {error}=await createClient().auth.signInWithPassword({email:String(f.get('email')),password:String(f.get('password'))});if(error)return setMsg(error.message);location.href='/dashboard'}return <main className="shell"><div className="auth card stack"><div className="brand">PRCUISA</div><h1>Welcome back</h1><form className="stack" onSubmit={submit}><input name="email" type="email" placeholder="Email" required/><input name="password" type="password" placeholder="Password" required/><button className="btn">Login</button></form>{msg&&<p className="muted">{msg}</p>}<p className="muted">New here? <Link href="/signup">Create account</Link></p></div></main>}
