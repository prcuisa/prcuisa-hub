'use client';
import Link from 'next/link'; import {useState} from 'react'; import {createClient} from '@/lib/supabase/client';
export default function Signup(){const [msg,setMsg]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const {error}=await createClient().auth.signUp({email:String(f.get('email')),password:String(f.get('password')),options:{data:{full_name:String(f.get('name'))}}});if(error)return setMsg(error.message);setMsg('Account created. Check your email, then login.')}return <main className="shell"><div className="auth card stack"><div className="brand">PRCUISA</div><h1>Create account</h1><form className="stack" onSubmit={submit}><input name="name" placeholder="Full name" required/><input name="email" type="email" placeholder="Email" required/><input name="password" type="password" minLength={8} placeholder="Password" required/><button className="btn">Create PRCUISA ID</button></form>{msg&&<p className="muted">{msg}</p>}<p className="muted">Already registered? <Link href="/login">Login</Link></p></div></main>}
